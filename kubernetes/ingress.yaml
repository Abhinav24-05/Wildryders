apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wildryde-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: wildryde.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wildryde-service
                port:
                  number: 80
